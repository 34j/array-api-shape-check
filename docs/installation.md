(installation)=

# Installation

The package is published on [PyPI](https://pypi.org/project/array-api-shape-check/) and can be installed with `pip` (or any equivalent):

```bash
pip install array-api-shape-check
```

Next, see the {ref}`section about usage <usage>` to see how to use it.
